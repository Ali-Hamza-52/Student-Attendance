export interface StudentFormValues {
    students: {
      studentName: string;
      rollNumber: string;
      gender: string;
      contactNumber: string;
      address: string;
      classId: string;
    }[];
  }